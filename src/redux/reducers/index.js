import { combineReducers } from "redux";
import calculator from "./calculatorReducer";


const rootReducer = combineReducers({
  calculator, // might add more later
  
});

export default rootReducer;